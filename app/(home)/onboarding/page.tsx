import OnboardingContainer from "@/components/onboarding/OnboardingContainer";

const page: React.FC = () => {
  const xml: React.ReactNode = (
    <>
      <OnboardingContainer />
    </>
  );
  return xml;
};

export default page;
