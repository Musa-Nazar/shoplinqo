import ConfirmationContainer from "@/components/confirmation/ConfirmationContainer";

const page: React.FC = () => {
  const xml: React.ReactNode = <ConfirmationContainer />;
  return xml;
};

export default page;
